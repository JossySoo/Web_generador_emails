import { FormatoTasaPipe } from './formato-tasa.pipe';

describe('FormatoTasaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTasaPipe();
    expect(pipe).toBeTruthy();
  });
});
